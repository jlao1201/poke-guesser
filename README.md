# PokeGuesser Discord Bot
Discord Bot that allows users to play "Guess that Pokemon" in Discord server text channels.

# Dependencies
- pip install discord.py
- pip install requests
- pip install bs4
- pip install pymongo
- pip install pymongo[srv]
- pip install fuzzywuzzy
